# 변경 내역

버전을 올릴 때 **서버에서 무엇이 달라지는지**를 적습니다. 코드 정리는 적지 않습니다.

---

## 1.4.0

- **`/wb check` — 백업에서 조용히 빠지는 월드를 찾아냅니다.**

  > 백업 대상은 `Bukkit.getWorlds()` 에서 옵니다. 그런데 그것은 **로드된 월드만** 돌려줍니다.
  > Multiverse 같은 플러그인이 만들어 두고 언로드한 월드, 플러그인이 로드에 실패해 올라오지
  > 못한 월드는 디스크에 멀쩡히 있으면서도 **백업에서 빠집니다.** 그런데 백업은 성공으로
  > 끝나고 경고 한 줄 없습니다 — 그 사실은 정작 되돌려야 하는 날에 드러납니다.
  >
  > 이제 서버 폴더를 직접 훑어 `level.dat` 이 있는 폴더를 모두 찾고, 백업이 담고 있는 것과
  > 견줍니다. 빠지는 월드가 있으면 이름과 **`extra-paths` 에 넣을 줄까지** 알려 줍니다.
  >
  > 폴더 이름이나 구조를 전제하지 않으므로 플러그인이 한글 이름을 붙여도, `--universe` 로
  > 한 단 들어가 있어도 찾습니다. `plugins/`·`libraries/` 처럼 월드가 있을 리 없는 곳은
  > 훑지 않습니다.

- **이 점검은 서버를 켤 때마다 자동으로 돕니다.** 명령을 기억해서 쳐야만 알 수 있다면
  대부분은 영영 모른 채로 지냅니다.

  > 특히 위험한 것이 **망가진 월드가 계속 백업되는** 상황입니다. 지형이나 플레이어 데이터가
  > 사라진 채로 백업이 계속 쌓이면, 멀쩡한 예전 백업이 보관 정책에 밀려 사라집니다.
  > 복원 실패 정지가 막으려는 것과 똑같은 연쇄입니다.
  >
  > 시작할 때 도는 검사는 **크기를 재지 않습니다.** 지형과 플레이어 데이터가 보이는 순간
  > 멈추므로 대개 파일 수십 개만 봅니다 — 월드 전체를 훑으면 그것만으로 부팅이 몇 초
  > 늘어나고, 그러면 관리자가 검사를 꺼 버립니다. 크기까지 보려면 `/wb check`.

- **월드에 무엇이 빠졌는지도 함께 알려 줍니다.** 지형 파일(`.mca`)이 없거나 플레이어 데이터
  폴더가 없으면 짚어 줍니다.

  > **없는 파일을 지어내지는 않습니다.** 특히 `level.dat` 은 절대 만들지 않습니다 — 그 안에
  > **시드**가 들어 있어서, 새로 만들면 서버가 다른 지형을 생성하기 시작합니다. 이미 있는
  > 청크는 그대로 남으므로 **땅이 어긋난 월드**가 되고 그것은 되돌릴 수 없습니다.
  > 없으면 백업에서 되돌리는 것이 유일하게 맞는 길입니다.

---

## 1.3.2

- **OneBack 이 자기 옛 사본에 막혀 갱신되지 못하던 것을 고쳤습니다.**

  > 아카이브 두 벌이 있는 서버에서 새 한 벌을 만들려면 잠깐 **세 벌만큼**의 자리가 필요합니다
  > (새것을 다 만든 뒤에야 오래된 것을 지우기 때문입니다). 할당량이 빠듯한 호스팅에서는 그
  > 순간을 넘기지 못해 공간 부족으로 거절되고, **영영 새 아카이브를 만들지 못합니다.**
  > 폴더에는 낡은 두 벌만 남고, 정작 서버를 잃는 날 몇 달 전 상태로 돌아가게 됩니다.
  >
  > 이제 자리가 모자라면 **가장 오래된 것을 먼저 내주고** 만듭니다. 보관 정책을 어기는 것이
  > 아니라 적용 시점만 앞으로 옮기는 것입니다 — 어차피 새것이 만들어지면 지워질 것이었습니다.
  > 결과적으로 `keep: 2` 면 두 벌이 번갈아 갱신됩니다.
  >
  > **마지막 한 벌은 절대 지우지 않습니다.** 그것을 지우고 만들다 실패하면 손에 아무것도
  > 남지 않는데, 이 파일은 서버가 통째로 사라졌을 때를 위한 것입니다. 그 경우에는 만들지 않고
  > 무엇을 하면 되는지 알려 줍니다.

- **콘솔 로그의 앞머리가 두 번 붙던 것을 없앴습니다.** `[WorldBackUp] [WorldBackUp] 복원을
  시작합니다` 처럼 나왔습니다. Paper 가 이미 플러그인 이름을 붙이는데 코드가 한 번 더 적고
  있었습니다.

---

## 1.3.1

- **`/wb update` 로 받은 jar 가 실제로는 갈아 끼워지지 않던 것을 고쳤습니다.**

  > 버킷은 시작할 때 `plugins/` 의 jar 마다 **같은 이름**의 파일이 `plugins/update/` 에 있는지
  > 보고, 있으면 그것으로 덮어씁니다. 그런데 받은 jar 를 `WorldBackUp.jar` 라는 이름으로
  > 놓고 있었습니다. 파일 이름에 버전이 들어가는 이 플러그인에서는(`WorldBackUp-1.1.2.jar`)
  > 이름이 언제나 어긋납니다.
  >
  > 그러면 아무 일도 일어나지 않습니다 — 받아 둔 jar 가 `plugins/update/` 에 **조용히
  > 눌러앉고**, 관리자는 업데이트했다고 믿은 채 옛 버전을 계속 돌리게 됩니다. 백업
  > 플러그인에서 "고쳐 놓은 결함을 그대로 안고 도는" 가장 나쁜 모양입니다.
  >
  > 이제 지금 돌고 있는 jar 와 **같은 이름**으로 놓습니다. 그래서 `WorldBackUp-1.1.2.jar`
  > 라는 파일 안에 1.3.1 이 들어 있게 됩니다. 헷갈리지만 이것이 버킷이 정한 방식이고,
  > 갈아 끼워지지 않는 것보다는 이름이 낡은 편이 낫습니다.
  >
  > (파일 이름을 최신으로 맞추고 싶으시면 서버를 끄고 직접 바꿔 두시면 됩니다. 그다음부터는
  > 그 이름을 따릅니다.)

---

## 1.3.0

> `1.1.2` 와 `1.2.0` 은 따로 배포되지 않았습니다. 아래 세 절의 내용이 **한꺼번에 1.3.0 으로**
> 나갑니다. `1.1.1` 을 쓰고 계셨다면 세 절을 모두 읽으시면 됩니다.

- **`/wb update` — 새 버전을 알아서 받아 둡니다.** 깃허브 릴리스를 확인하고, 새 것이 있으면
  받아서 `plugins/update/` 에 놓습니다. 다음에 서버를 켜면 스스로 갈아 끼워집니다.

  > 돌고 있는 jar 는 건드리지 않습니다. 그 자리에서 바꾸면 윈도우에서는 잠겨서 실패하고,
  > 리눅스에서는 성공해도 이미 올라간 클래스는 그대로라 **반쯤 새 버전인** 서버가 됩니다.
  > 버킷이 마련해 둔 `plugins/update/` 를 쓰면 옛 jar 가 남는 문제까지 함께 사라집니다.
  >
  > 받은 파일을 그대로 믿지 않습니다 — `https` 인지, 깃허브 주소인지, 정말 zip 인지, 안의
  > `plugin.yml` 이 WorldBackUp 이고 버전이 맞는지 확인한 뒤에야 놓습니다. 하나라도
  > 어긋나면 지웁니다. 잘못된 파일이 `plugins/` 에 놓이면 다음 부팅에 서버가 아예 뜨지
  > 않기 때문입니다.

- **시작할 때 새 버전이 있는지 알려 줍니다.** (`update.check`, 기본 켜짐) 확인만 하고
  내려받지는 않습니다. 인터넷이 없는 서버에서 실패해도 조용히 넘어갑니다.

- **`update.auto-download`** 를 켜면 그 확인에서 자동으로 받아 둡니다. 기본값은 꺼짐입니다 —
  켜면 서버가 앞으로 그 저장소의 코드를 사람 확인 없이 받아 실행하게 되므로, 그 선택은
  관리자가 직접 하는 편이 맞다고 보았습니다.

- **OneBack 폴더에 압축 풀기 도우미가 함께 놓입니다.** `unpack.bat`(윈도우) ·
  `unpack.sh`(리눅스) 를 실행하면 새 폴더를 만들어 알아서 풀어 줍니다.

  > "zip 을 빈 폴더에 푸세요" 는 생각보다 큰 벽입니다. 윈도우 탐색기에서 zip 을
  > 더블클릭하면 **푼 것처럼 보이지만 안을 들여다본 것뿐**이고, 그 상태로 파일을 끌어다
  > 놓으면 몇 GB 를 옮기다 멈추기도 합니다. 서버가 안 뜨는 이유가 그것인지 알아채기도
  > 어렵습니다.
  >
  > 이미 있는 폴더에는 절대 풀지 않습니다. 돌아가는 서버를 덮어쓰는 것이 이 도우미가 만들
  > 수 있는 가장 나쁜 결과입니다.

---

## 1.2.0

**OneBack — 이 폴더 하나만 챙기면 아무 데서나 서버를 다시 열 수 있습니다.**

- **`/wb oneback` 이 서버 폴더를 통째로 담습니다.** 서버 jar, `eula.txt`, `server.properties`,
  모든 월드, **모든 플러그인(jar 와 데이터)**, 설정까지 전부입니다.

  > 평소 백업과 목적이 다릅니다. 그쪽은 "이 서버를 어제로 되돌리기" 라 서버 폴더가 이미
  > 있다고 보고 월드와 설정만 담습니다. 서버 폴더가 통째로 사라지는 상황 — 디스크 고장,
  > 호스팅 계정 정지, 실수로 삭제, 업체 이전 — 에는 그것만으로 부족합니다. 서버 jar 도
  > 플러그인 jar 도 없기 때문입니다.

  만들어진 `OneBack/` 폴더에는 zip 과 함께 **`읽어주세요.txt`** 가 들어갑니다. 다시 여는
  절차가 적혀 있어, 몇 달 뒤 그 폴더만 발견한 사람이 zip 을 풀지 않고도 복구할 수 있습니다.

  > `libraries/` 와 `versions/` 도 **일부러 담습니다** — 그게 있어야 인터넷 없이도 그 자리에서
  > 서버가 뜹니다. 로그와 크래시 기록, 그리고 백업 폴더 자신은 빠집니다(아카이브가 아카이브를
  > 삼키지 않도록).
  >
  > 기본값은 **수동(`interval-hours: 0`)** 입니다. 한 번에 서버 폴더 크기만큼 쓰기 때문에,
  > 자동으로 도는 것이 기본이면 디스크가 작은 서버에서 관리자가 알아채기 전에 찹니다.
  > 자동으로 돌리시려면 `oneback.interval-hours` 를 24 정도로 두고 `keep` 을 1~2 로 잡으세요.

- **OneBack 이 자동 백업의 복원 지점을 빼앗던 것을 고쳤습니다.** OneBack 은 월드를 저장하면서
  "변경 없음" 표시를 함께 내렸는데, 그러면 다음 자동 백업이 건너뛰어집니다. 그 사이의 변경은
  OneBack 안에만 남고, OneBack 은 `/wb restore` 목록에 오르지 않으므로 **그 시점으로 되돌릴
  방법이 사라졌습니다.** 이제 복원 지점을 만드는 평소 백업만 그 표시를 내립니다.

- **OneBack 도 시작 전에 디스크 여유를 확인합니다.** 서버 폴더 크기만큼을 한 번에 쓰는,
  이 플러그인에서 디스크를 가장 많이 먹는 작업인데 점검이 없었습니다. 도중에 디스크가 차면
  못 쓰는 조각만 남는 것이 아니라 **서버가 멈춥니다** — 마인크래프트는 청크를 쓰지 못하면
  그 자리에서 죽습니다. 모자라면 아무것도 만들지 않고 무엇을 하면 되는지 알려 줍니다.

- **중간에 끊긴 OneBack 조각을 시작할 때 치웁니다.** 압축 중에 서버가 죽으면 `.zip.tmp` 가
  남는데, 그게 **서버 폴더 크기만 합니다.** 평소 백업의 조각 정리는 백업 폴더만 보기 때문에
  아무도 치우지 않아 디스크에 서버 한 벌이 영영 눌러앉았습니다.

- **`/wb status` 에 OneBack 이 보입니다.** 몇 개가 있고 가장 최근 것이 언제인지 나옵니다.
  없으면 "아직 없음" 이라고 알려 줍니다. 재해 복구용 파일에서 정작 알고 싶은 것이
  "있느냐, 얼마나 오래됐느냐" 인데, 그걸 보려고 서버 폴더를 뒤져야 하면 대개 보지 않습니다.

- **플러그인 폴더(`plugins/`)는 평소 백업에도 함께 담깁니다.** 다른 플러그인의 jar 와 데이터
  (경제 잔고, 보호구역, 이벤트 기록 등)가 모두 들어갑니다. `targets.plugins` 로 조절합니다.

  > 월드만 되돌리면 플러그인이 들고 있는 상태는 그대로 남아 월드와 어긋납니다. 되돌린
  > 시점에는 없던 상점 거래가 살아 있고, 사라진 건물의 보호구역이 그대로 남는 식입니다.
  >
  > jar 는 담기지만 **복원할 때는 덮어쓰지 않습니다.** 사흘 전으로 되돌릴 때 원하는 것은
  > 대개 "지금 코드 + 그때 데이터" 이지 사흘 전 버전의 플러그인이 아닙니다.

---

## 1.1.2

**복원해도 op 목록이 바뀌지 않던 것을 고쳤습니다.** 설정을 바꿀 필요는 없습니다.

- **업데이트는 jar 만 바꾸면 됩니다. 그것만으로 서버 동작이 달라지지 않습니다.**

  > 지금까지는 그렇지 않았습니다. 1.0.0 서버에 jar 만 올리면 새 기본값이 그대로 들어와서,
  > 서버를 켜고 **몇 초 뒤 도는 첫 보관 정리에서 백업 48개 중 28개가 지워졌습니다.**
  > `retention.tiers`(계단식 보관)가 파일에 들어오면서 그때까지 쓰던
  > `max-backups`/`max-age-days`/`keep-daily` 를 대신했기 때문입니다. 옛 값은 파일에
  > 그대로 남아 있어서, 관리자가 열어 봐도 여전히 48개를 지키는 것처럼 보였습니다.
  > `targets.plugins` 도 마찬가지로 기본값이 `all` 이라, 아무것도 안 했는데 다음 백업부터
  > `plugins/` 가 통째로 들어가 크기가 뛰었습니다.

  이제 **동작을 바꿀 수 있는 설정은 "지금 그대로" 인 값으로 들어옵니다.** 새로 설치하는
  서버는 예전처럼 새 기본값을 받습니다. 이미 돌던 서버는 관리자가 읽고 켜기 전까지 하던
  대로 돕니다. 무엇이 꺼진 채로 들어왔는지는 시작할 때 콘솔에 뜨고, **켜는 방법이 config.yml
  그 자리에 주석으로** 들어갑니다 — 주석만 풀면 됩니다.

  > 1.0.0 서버를 그대로 만들어 놓고 확인했습니다: 백업 48개 → 업데이트 → **삭제 0개,
  > 48개 전부 유지.** 옛 백업(전체·차등 모두)은 그대로 복원되고, `/wb lock` 으로 잠근
  > 백업도 남습니다.

- **`plugins/` 에 WorldBackUp jar 가 두 개 이상이면 시작할 때 크게 알립니다.** 파일 이름에
  버전이 들어 있어서 새 jar 를 넣기만 하면 옛 jar 가 남는데, 그러면 어느 쪽이 올라올지는
  서버가 폴더를 읽는 순서에 달립니다. 업데이트한 줄 알았는데 옛 버전이 돌고 있을 수 있고,
  그 사실을 알 방법이 없었습니다. (지우지는 않습니다 — 어느 것이 옛 것인지는 사람이 봐야 합니다)

- **되돌려도 op·화이트리스트가 그대로였습니다.** 서버는 `ops.json` 같은 파일을 플러그인보다
  **먼저** 읽어 메모리에 올려 둡니다. 복원은 월드 파일을 안전하게 교체하려고 그보다
  뒤(`onLoad`)에서 도니, 파일은 백업 시점으로 되돌아가는데 **서버가 들고 있는 목록은
  복원 전 그대로**였습니다. `/wb list` 도 복원 보고서도 "성공" 이라고 말하는데, 정작 op 는
  하나도 달라지지 않는 상태였습니다.

  > 조용히 넘어가지도 않았습니다. 서버는 `/op` `/deop` 한 번에 **메모리를 파일로 덮어씁니다.**
  > 그래서 복원이 되돌려 놓은 `ops.json` 은 다음 `/op` 한 번에 사라졌고, 재시작해도 돌아오지
  > 않았습니다 — 복원이 아예 없었던 것이 됩니다. 하필 op 를 잃은 관리자가 가장 먼저 하게 되는
  > 것이 그 명령입니다.

  이제 월드가 올라온 직후 서버가 들고 있는 목록을 **백업 시점으로 맞춥니다.** 누가 op 가 되고
  누가 내려갔는지는 콘솔에 이름으로 남습니다. 파일 내용(이름, `level`)은 백업에 담겼던
  그대로 유지됩니다.

  > 목록 파일을 읽지 못하면(JSON 이 깨진 경우 등) 아무것도 건드리지 않고 크게 알립니다 —
  > "읽지 못했다" 를 "아무도 없었다" 로 받아들이면 전원을 op 에서 내리게 되고, 그러면 관리자가
  > 게임 안에서 되돌릴 방법이 없습니다.
  >
  > 복원한 백업에 op 가 아무도 없던 시점이면 실제로 전원이 내려갑니다. 그것이 그 백업의
  > 내용이기 때문입니다. 이때는 콘솔에서 `op <이름>` 으로 올려 주세요.

- **밴 목록은 이제 복원해도 되돌아가지 않습니다.** 밴한 사람은 복원 뒤에도 그대로 밴입니다.

  > 이 플러그인을 쓰는 가장 흔한 순간이 "테러범을 밴하고 그 전으로 되돌리는" 것입니다.
  > 그런데 `banned-players.json` 까지 백업 시점으로 되돌리면 **방금 건 밴이 함께 풀립니다.**
  > 되돌리자마자 같은 사람이 다시 들어올 수 있는 셈이라, 복원이 사고를 반쯤 되살립니다.
  > 이제 `banned-players.json` 과 `banned-ips.json` 은 복원이 아예 덮어쓰지 않습니다.
  >
  > 백업에는 계속 담깁니다 — 서버를 통째로 잃었을 때 밴 목록도 함께 잃으면 안 되니까요.
  > 옛 밴 목록이 필요하면 백업 zip 에서 직접 꺼내 쓰면 됩니다.

- **`server.properties` 처럼 되돌려도 이번 세션에는 적용할 수 없는 파일은 이름을 대며
  알립니다.** (`bukkit.yml`, `spigot.yml`, `permissions.yml` 등) 서버를 한 번 더 껐다 켜면
  적용됩니다. 전에는 이것도 조용해서, 난이도나 화이트리스트 사용 여부가 예전 그대로인 것을
  나중에 사고로 알게 됐습니다.

- **백업 중 "The server has not responded for 10 seconds" 가 뜨던 것을 줄였습니다.**

  청크 저장을 기다리는 동안 서버가 멈추는 것 자체는 양보할 수 없습니다. 기다리지 않으면
  서버가 아직 쓰고 있는 region 파일을 압축하게 되어, 복원할 때 `Corrupt regionfile header`
  가 뜨고 복구되지 않은 청크는 통째로 재생성됩니다.

  문제는 **월드 셋을 한 틱에 몰아 저장하던 것**이었습니다. 워치독은 "틱 하나" 가 얼마나
  걸렸는지를 보므로, 월드마다 4초씩만 걸려도 12초짜리 틱이 되어 경고와 스레드 덤프가
  찍힙니다. 그 덤프에는 이 플러그인의 스택이 남아서 관리자는 이쪽을 의심하게 되고,
  합이 워치독의 **치명 임계**에 닿으면 서버가 강제로 죽습니다.

  이제 두 가지가 달라집니다.

  **하나 — 월드마다 틱을 나눠 저장합니다.** 멈추는 총량은 같지만 한 번에 멈추는 길이가
  월드 수만큼 줄어듭니다.

  **둘 — 기다리기 전에 쓰기를 미리 걸어 둡니다.** (`backup.flush-settle-seconds`, 기본 3초)
  저 스택의 `linearLongBackoff` 는 청크를 만드는 중이 아니라 **이미 큐에 쌓인 쓰기가
  디스크로 내려가기를 기다리는 중**입니다. 그래서 기다리지 않는 저장으로 쓰기를 먼저 걸어
  두고, 서버가 정상적으로 도는 동안 I/O 스레드가 그것을 내려쓰게 둡니다. 이 몇 초 동안
  **서버는 멈추지 않습니다** — 잠드는 것은 백업 스레드뿐입니다. 그 뒤의 실제 저장은 그
  사이에 새로 더러워진 청크만 기다리면 됩니다.

  > 디스크가 빠르면 이 예열은 아무것도 줄이지 못합니다. NVMe·45MB 월드에서 재 보니
  > 예열 없이 76ms, 예열하고 74ms 로 차이가 없었습니다 — 그 환경에서 저 시간은 기다림이
  > 아니라 로드된 청크를 훑는 비용입니다. 이 기능이 겨냥하는 것은 그 기다림이 **초 단위**로
  > 늘어나는 환경(느린 디스크, 컨테이너 호스팅의 I/O 제한, 청크가 아주 많은 월드)이고,
  > 필요 없으면 `0` 으로 끄면 됩니다.

  > 로그도 월드별로 바뀌었습니다. `[백업] 청크 저장을 기다렸습니다 · world: 2.8초` 처럼
  > 어느 월드가 비싼지 나오고, 월드가 여럿이면 합계도 함께 찍힙니다. 한 월드가 10초를
  > 넘는다면 주기를 줄이는 것으로는 해결되지 않습니다 — 그 월드를 `targets.worlds` 에서
  > 빼거나, 디스크 쪽을 보셔야 합니다.
  >
  > 참고로 **보관 개수를 줄이는 것은 이 증상과 무관합니다.** 오래된 백업 삭제는 백업이
  > 끝난 뒤 비동기 스레드에서 돌고, 메인 스레드를 잡지 않습니다.

- **`plugins/` 폴더를 함께 백업합니다.** (`targets.plugins`, 기본 `all`)

  월드만 되돌리면 경제 잔고·보호구역·권한처럼 **플러그인이 들고 있는 상태**는 그대로 남아
  월드와 어긋납니다. 되돌린 시점에는 없던 상점 거래가 그대로 살아 있고, 사라진 건물의
  보호구역이 그대로 남습니다. 전에는 `targets.extra-paths` 에 직접 적어야 했고, 적어야
  한다는 사실을 알아채는 시점이 대개 되돌린 뒤였습니다.

  > **jar 는 담기지만 복원 때 덮어쓰지 않습니다.** 복원은 월드가 올라오기 전(`onLoad`)에
  > 도는데, 그 시점에는 서버가 이미 모든 플러그인 jar 를 열어 둔 뒤입니다. 윈도우에서는
  > 파일이 잠겨 실패하고, 리눅스에서는 성공해도 이번 세션에는 아무 효과가 없이 **다음
  > 재시작에 조용히 옛 버전으로 돌아갑니다.** 게다가 사흘 전으로 되돌릴 때 원하는 것은 대개
  > "지금 코드 + 그때 데이터" 이지 사흘 전 버전의 플러그인이 아닙니다. 서버를 통째로 잃었을
  > 때 쓸 수 있도록 담기기는 하므로, 필요하면 zip 에서 직접 꺼내면 됩니다.
  >
  > jar 를 아예 담지 않으려면 `targets.plugins: data`, 예전처럼 두려면 `none` 입니다.
  > `data` 는 `plugins/` **바로 아래**의 jar 만 뺍니다 — 플러그인이 자기 폴더 안에 두는
  > 라이브러리 jar 는 그 플러그인의 데이터라 함께 담습니다.
  >
  > 복원은 이 폴더도 "백업 시점 그대로" 만듭니다. 백업한 뒤에 설치한 플러그인의 **데이터
  > 폴더는 사라집니다**(jar 는 위 규칙으로 남습니다). `restore.keep-replaced-files` 가 켜져
  > 있으면 `plugins/WorldBackUp/replaced/` 로 옮겨 두므로 되찾을 수 있습니다.
  >
  > 데이터베이스를 열어 둔 채 쓰는 플러그인(SQLite/H2 등)은 백업 순간의 파일이 잘려 있을 수
  > 있습니다. 그런 폴더는 `targets.exclude` 에 넣으세요.
  >
  > 이미 `targets.extra-paths` 에 `plugins/LuckPerms` 처럼 적어 두셨다면 **그대로 두셔도
  > 됩니다.** 위 설정이 이미 담고 있으면 말없이 합쳐집니다. (전에는 이런 겹침을 백업할 때마다
  > 알렸는데, 30분 주기면 하루 48줄이라 진짜 경고까지 함께 묻혔습니다. `plugins/` 밖의
  > 겹침은 예전처럼 알립니다.)

- **새 설정이 기존 서버의 `config.yml` 에도 저절로 들어갑니다.**

  전에는 플러그인만 새 버전으로 갈아 끼우면 `config.yml` 은 그대로였습니다. 버킷이 그 파일을
  **없을 때만** 쓰기 때문입니다. 그래서 위 `targets.plugins` 같은 설정은 코드 기본값으로
  동작만 하고 파일에는 나타나지 않았고, 관리자는 그런 설정이 생긴 줄도 무엇을 뜻하는지도
  알 수 없었습니다. 이 플러그인은 설명을 전부 그 파일의 주석에 적어 두기 때문에 더 그렇습니다.

  이제 시작할 때 빠진 설정을 **설명 주석째** 끼워 넣고 콘솔에 이름을 남깁니다.
  `[WorldBackUp] 새로 생긴 설정을 config.yml 에 추가했습니다 (설명 포함): targets.plugins`

  > **여러분이 쓴 줄은 한 줄도 건드리지 않습니다.** 바꾼 값도, 직접 단 주석도, 순서도,
  > 줄 끝(CRLF/LF)도 그대로 두고 빠진 것만 끼웁니다. 넣는 자리도 배포본을 따라가므로
  > 새 설정이 자기 설명과 함께 제자리에 들어갑니다 — 파일 끝에 몰아넣지 않습니다.
  >
  > 흔히 쓰는 방식(`copyDefaults`)은 값은 채워 주지만 **주석을 전부 날리고** 순서를
  > 뒤섞습니다. 이 파일은 대부분이 설명이라 그러면 남는 것이 없어 쓰지 않았습니다.
  >
  > 합친 결과가 YAML 로 읽히지 않으면 파일에 손대지 않고, 실패해도 서버 시작을 막지
  > 않습니다. 설정이 파일에 없어도 기본값으로 도는 데는 문제가 없기 때문입니다.
  >
  > 옛 백업을 복원하면 이 파일도 그 시점으로 돌아가 새 설정이 다시 사라지는데,
  > 다음 시작에서 다시 채워 넣습니다.

- **백업 대상 폴더와 이름이 겹치는 형제 폴더를 자식으로 읽던 자리를 막았습니다.**
  뿌리가 `plugins` 일 때 `plugins-old/x` 는 문자열로는 접두사가 맞아서, 이어 붙이기로
  처리하면 `plugins/old/x` 라는 있지도 않은 경로가 나왔습니다. 그 이름으로 담기면 복원이
  엉뚱한 자리에 씁니다.

  > 지금 코드가 그 상황을 만들지는 않습니다(순회가 내놓는 경로만 들어옵니다). 그래도 막아
  > 둔 이유는 실패가 조용하기 때문입니다 — 예외도 경고도 없이 이름만 틀린 채로 백업이
  > 성공합니다.

---

## 1.1.1

`1.1.0` 을 훑어 찾은 결함을 고쳤습니다. 설정을 바꿀 필요는 없습니다.

- **심볼릭 링크로 걸어 둔 월드가 백업에 한 파일도 담기지 않았습니다.** 월드를 다른 디스크에
  두고 서버 폴더에서 링크로 거는 것(월드는 SSD, 서버는 HDD)은 리눅스 서버에서 흔한 구성인데,
  그런 서버의 백업은 **지형도 플레이어 데이터도 없이 성공한 것처럼** 끝났습니다.
  복원할 때가 되어서야 알게 되는 최악의 실패입니다.

  > 복원 쪽도 같은 이유로 링크 <b>자체</b>를 `replaced/` 로 치우고 그 자리에 압축을 풀었습니다.
  > 다른 디스크에 두려고 걸어 둔 링크가 복원 한 번에 사라지고 월드가 시스템 디스크에 새로
  > 만들어지는 것입니다. 이제 링크를 따라 들어가 안을 비우고, 링크는 그대로 남습니다.

- **`/wb lock` 으로 잠근 백업이 손상으로 보이면 보호를 잃고 지워졌습니다.** README 는 "어떤
  정책으로도 지워지지 않습니다" 라고 약속하는데 지키지 못했습니다. "손상" 에는 *메타데이터를
  읽지 못했다* 가 포함되고 그것은 일시적일 수 있어서(네트워크 저장소, 외부 동기화 중),
  영구 보관하려고 잠근 백업이 조용히 사라질 수 있었습니다. 잠금 마커는 사이드카와 별개
  파일이라 그 상황에서도 읽히므로, 이제 그 의사를 우선합니다.

  > 이것은 예전 결정을 **바꾼** 것입니다. 전에는 "복원에 못 쓰는 백업을 정책이 지켜 줄 이유가
  > 없다" 고 보았습니다. 두 실패의 대가가 비대칭이라 바꿨습니다 — 잘못 지우면 되돌릴 수 없고,
  > 잘못 남기면 `/wb list` 에 `[보호] [손상]` 으로 보이며 `/wb unlock` 후 지울 수 있습니다.
  > 손상된 백업을 **새로** 잠그는 것은 그대로 거부합니다.

- **보관 정리가 "전부 손상으로 보인다" 는 이유로 백업 폴더를 통째로 비울 수 있었습니다.**
  "손상" 판정에는 *사이드카와 zip 메타를 둘 다 못 읽었다* 가 포함됩니다 — 네트워크 저장소의
  일시적인 I/O 오류이거나, 백업 폴더를 밖으로 동기화하는 중(zip 은 도착했고 사이드카는 아직)
  일 수도 있습니다. 백업본을 외부로 복사하는 것은 이 플러그인이 권하는 운영 방식이라 실제로
  일어납니다. 공간 확보 경로는 이 규칙을 지키고 있었는데 정리 경로에만 빠져 있었고, 하필
  계단식이 기본값이라 **기본 설정에서** 걸렸습니다. 이제 마지막 하나는 남기고 크게 알립니다.
  (읽을 수 있는 백업이 있었다면 `min-backups: 0` 같은 명시적 설정은 그대로 존중합니다)

- **`/wb prune` 이 복원 실패 정지를 무시했습니다.** 복원이 실패하면 플러그인은 자동 백업과
  보관 정리를 멈춥니다 — 반쯤 복원된 월드가 백업되면서 멀쩡한 예전 백업이 정책에 밀려
  사라지는 것을 막기 위해서입니다. 자동 주기·백업 뒤 정리·시작 시 정리·공간 확보는 모두 이
  정지를 지키는데, **정작 그 정책을 직접 부르는 `/wb prune` 만 검사를 빠뜨리고 있었습니다.**
  하필 디스크가 모자란 상황에서 가장 먼저 떠올리는 명령입니다. 이제 거부하고 무엇을 해야
  하는지 알려 줍니다. (`/wb delete [ID]` 는 지울 대상을 직접 지목하므로 그대로 쓸 수 있습니다)
- **`min-free-disk-gb` 에 소수점을 적으면 조용히 0 이 됐습니다.** `0.5` 라고 적어 512MB 를
  남겨 두려던 설정이 "여유 없음" 으로 바뀌어, 디스크 브레이크가 사실상 꺼졌습니다.
  경고도 없어서 백업이 디스크를 끝까지 채운 뒤에야 드러났습니다. 이제 소수점을 받습니다.
- **제외 패턴을 윈도우식(`world\region\**`)으로 적으면 아무것도 걸러 내지 않았습니다.**
  검사할 경로는 `/` 로 맞추면서 패턴은 그대로 뒀기 때문입니다. 조용히 통과해서, 큰 폴더를
  백업에서 뺐다고 믿는 쪽이 실제로는 그대로 담고 있었습니다. 이제 패턴도 같이 정규화합니다.
- **`retention.tiers` 를 항목 형태가 아니게 적으면 경고 없이 옛 정책으로 돌아갔습니다.**
  계단이 도는 줄 알았는데 실제로는 `max-backups`/`max-age-days` 가 돌고 있고, 그 사실은
  정작 되돌려야 하는 날에야 드러납니다. 이제 시작할 때 콘솔에 알립니다.
- **복원 요청과 `/wb confirm` 사이에 자동 백업 주기가 걸리면** 안전 백업이 "이미 백업이
  진행 중" 으로 실패하면서 복원이 중단됐고, 그 메시지만으로는 원인을 알 수 없었습니다.
  이제 확정 단계에서 먼저 알리고 요청을 살려 둡니다 — 백업이 끝나면 `/wb confirm` 만 다시
  입력하면 됩니다.
- 안전 백업이 실패해 복원이 막혔을 때, 빠져나갈 길(`restore.create-safety-backup: false`)을
  함께 알려 줍니다. 되돌릴 방법이 아예 없는 것처럼 보이던 화면이었습니다.

- 백업 폴더를 **`plugins/` 밖에 두는 법**을 문서에 적었습니다. 설정으로는 원래 되던 일이지만
  상대 경로 기준이 `plugins/WorldBackUp/` 이라 예시가 없으면 알기 어려웠습니다
  (`directory: "../../worldbackups"` → 서버폴더/worldbackups/). 플러그인을 지우거나 다시
  설치할 때 백업이 함께 날아가지 않습니다.

---

## 1.1.0

`1.0.0` 이후의 안정성 강화를 모았습니다. **config.yml 은 그대로 쓰셔도 됩니다** — 플러그인은
설정 파일이 없을 때만 새로 쓰므로, 기존 값과 동작이 유지됩니다.

### 업그레이드하면 바뀌는 것

- **백업에 이 플러그인의 `config.yml` 이 함께 담깁니다.** 담지 않으면 서버를 되돌려도 그 시점의
  보관 정책은 되돌아오지 않아서, 백업이 어떻게 만들어졌는지가 정작 백업 안에 없었습니다.

  > 그래서 **복원하면 `config.yml` 도 그 시점 것으로 바뀝니다.** `backup.directory` 처럼 그 뒤에
  > 고친 값이 예전으로 돌아갈 수 있으니 복원 후 `/wb status` 로 한 번 확인하세요.
  > 원치 않으면 `targets.exclude` 에 `"plugins/WorldBackUp/config.yml"` 을 넣으면 됩니다.
  > 이 버전 이전에 만든 백업으로 복원할 때는 예전처럼 `config.yml` 을 건드리지 않습니다.

- 반대로 플러그인이 **스스로 만들어 쓰는 것**(`backups/`, `replaced/`, 복원 예약 파일, 실패 표식)은
  절대 백업에 담기지 않습니다. 담기면 복원이 그것을 되살리는데, 복원 예약이 되살아나면 다음 부팅이
  **또 복원을 시작하고**, 실패 표식이 되살아나면 자동 백업이 영구히 멈춥니다.
- 기본 보관 정책이 **계단식(`retention.tiers`)** 으로 바뀌었습니다. 새로 설치하는 서버에만 적용되며,
  최근일수록 촘촘히 남겨 "자는 동안 당한 테러를 아침에 발견" 하는 상황을 견딥니다. (기본값 27개 / 약 16일)
- 기본 백업 방식이 **차등(`backup.mode: differential`)** 으로 바뀌었습니다. 새로 설치하는 서버에만
  적용됩니다. `full` 을 쓰면서 깊은 계단을 함께 설정한 서버는 시작할 때 콘솔에 경고가 뜹니다.

### 새로 생긴 것

- `retention.min-backups` — 어떤 정책으로도 이 개수 아래로 줄이지 않는 하한선입니다. 무인 서버가
  오래 놀면 나이 정책 하나로 백업이 전멸할 수 있었습니다. **0 으로 두지 마세요.**
- `retention.max-total-size-gb` — 백업 폴더 총 용량 상한. 컨테이너 호스팅에서는
  `min-free-disk-gb` 가 호스트 디스크를 보므로 정작 걸려 있는 할당량을 못 봅니다.
- `backup.max-skipped-cycles` — 변경이 없어 보여도 이 횟수를 채우면 한 번은 백업합니다.
  "변경 없음" 판단은 블록 설치·파괴·접속만 보므로, 강제 로드된 청크의 농장은 잡히지 않습니다.
- `retention.max-protected` — 수동·복원 직전 백업이 무한히 쌓이지 않게 하는 상한.
- `restore.keep-replaced-max` — `replaced/` 스냅샷 보관 개수. 복원 한 번에 월드 한 벌이 쌓입니다.
- `/wb restore` (인자 없이) — 되돌릴 시점을 클릭으로 고르는 메뉴. 각 줄이 실제로 존재하는 백업을
  가리키므로 눌렀을 때 어디로 돌아가는지가 화면에 그대로 보입니다.
- `/wb restore at [시각]` — `03:00`, `2026-08-16 03:00`, `9h` 로 되돌릴 시점을 찾습니다.
  모두 **"그 시점 이전"** 을 고릅니다. 사고 시각 이후 백업이 잡히면 피해가 담긴 상태로 돌아가기 때문입니다.
- `/wb list days` — 날짜별 보유 현황. 계단식이 실제로 도는지도 이 화면에서 드러납니다.
- `/wb lock` · `/wb unlock` — 자동 삭제로부터 보호. 어떤 정책보다 우선합니다.
- 탭 완성이 종류별로 묶여 나오고, **각 줄에 그것이 실제로 가리키는 백업**이 적힙니다.

### 고친 것

- **인벤토리가 백업에 들어가지 않던 문제.** 26.2 는 `getWorldFolder()` 가 차원 폴더
  (`world/dimensions/minecraft/overworld`)를 돌려주는데, 거기만 담으면 지형은 되돌아오지만
  `playerdata` · `level.dat` · `data` 가 통째로 빠지고 **그런데도 백업은 성공한 것처럼 보였습니다.**
  이제 실제 월드 폴더까지 올라가고, 플레이어 데이터 위치를 전제하지 않고 찾습니다.
  백업에 인벤토리가 들었는지 기록해 `/wb list` · 복원 확인 화면 · `/wb status` 에 표시합니다.
- **서버가 아직 쓰고 있는 region 파일을 압축하던 문제.** 청크 쓰기가 디스크에 끝날 때까지
  기다립니다. 기다리지 않으면 헤더와 데이터가 어긋난 조각이 담겨, 복원 시
  `Corrupt regionfile header` 가 뜨고 그 청크가 재생성됐습니다.
- **백업 중 읽지 못한 파일을 "담았다" 고 기록하던 문제.** 잠긴 파일(`session.lock`, 서버가 쓰는
  중인 region)은 열기는 되고 읽기에서 실패합니다. 이제 담아낸 파일만 목록에 오르므로,
  복원이 멀쩡한 파일을 빈 파일로 덮어쓰지 않고 다음 차등 백업이 그 파일을 빠뜨리지 않습니다.
- **복원이 공간 부족으로 반쯤 진행되던 문제.** 복원은 "비우고 푸는" 순서라 도중에 디스크가 차면
  반쯤 복원된 월드만 남았습니다. 이제 지우기 전에 필요한 양을 정확히 계산해서, 모자라면 월드를
  건드리기 전에 멈춥니다. `/wb restore` 확인 화면에도 필요/남은 공간이 표시됩니다.
- **복원이 실패한 뒤 멀쩡한 예전 백업이 사라지던 문제.** 실패하면 `restore-failed-*.yml` 을 남기고
  그동안 자동 백업과 보관 정리를 멈춥니다. 반쯤 복원된 월드가 계속 백업되면서 되돌릴 수 있는
  백업을 정책으로 밀어내던 연쇄를 끊습니다.
- **총 용량 상한이 최신 백업을 지우던 문제.**
- **차등 백업의 기준이 삭제되어 갓 만든 차등본이 복원 불가가 되던 문제.** 압축 중에는 기준을
  고정하고, 보관 정책·공간 확보·수동 삭제 어느 쪽으로도 지우지 않습니다.
- **쓰다 만 백업 메타데이터를 정상으로 믿던 문제.** 차등 백업이 전체 백업으로 둔갑해, 복원이
  대상 경로를 좁히지 못한 채 바뀐 파일 몇 개를 살아 있는 월드 위에 흩뿌렸습니다.
- **백업이 실패하면서 멀쩡한 백업을 지우던 문제.** 압축이 끝난 뒤 뒷정리에서 난 예외가 방금 만든
  백업을 지우고, 성공한 백업을 실패로 보고했습니다.
- 백업이 어떤 이유로 실패해도(타임아웃 포함) 월드 **자동 저장이 반드시 원복**됩니다.
  꺼진 채 방치되면 서버가 크래시할 때 그 세션 전체가 날아갑니다.
- `worlds` 만 복원할 때 월드를 못 찾으면 전체를 덮어쓰지 않고 거부합니다.
- 백업에 내용이 없는 경로는 비우지 않고 건너뜁니다. (비우기만 하면 그 폴더가 사라집니다)

### 빨라진 것

- 이미 압축된 형식(`.mca`, `.zip`, `.png` 등)은 재압축하지 않습니다. 결과물 크기는 사실상 그대로이고
  백업 시간은 **8배** 짧아졌습니다(31MB 월드: 753ms → 94ms).
- 차등 백업이 대상을 두 번 훑지 않습니다. 공간이 넉넉하다고 단정할 수 있으면 용량 측정 순회를
  건너뜁니다(파일 2만 개 월드에서 114ms → 79ms).
- `/wb list` · `/wb status` · 탭 완성이 매번 백업 폴더를 다시 읽지 않습니다.

---

## 1.0.0

첫 공개. 주기적 백업, 시점 복원, 차등 백업, 보관 정책, `/wb` 명령어.
