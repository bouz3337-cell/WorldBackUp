# 변경 내역

버전을 올릴 때 **서버에서 무엇이 달라지는지**를 적습니다. 코드 정리는 적지 않습니다.

---

## 1.1.1

`1.1.0` 을 훑어 찾은 결함을 고쳤습니다. 설정을 바꿀 필요는 없습니다.

- **심볼릭 링크로 걸어 둔 월드가 백업에 한 파일도 담기지 않았습니다.** 월드를 다른 디스크에
  두고 서버 폴더에서 링크로 거는 것(월드는 SSD, 서버는 HDD)은 리눅스 서버에서 흔한 구성인데,
  그런 서버의 백업은 **지형도 플레이어 데이터도 없이 성공한 것처럼** 끝났습니다.
  복원할 때가 되어서야 알게 되는 최악의 실패입니다.

  > 복원 쪽도 같은 이유로 링크 <b>자체</b>를 `replaced/` 로 치우고 그 자리에 압축을 풀었습니다.
  > 다른 디스크에 두려고 걸어 둔 링크가 복원 한 번에 사라지고 월드가 시스템 디스크에 새로
  > 만들어지는 것입니다. 이제 링크를 따라 들어가 안을 비우고, 링크는 그대로 남습니다.

- **보관 정리가 "전부 손상으로 보인다" 는 이유로 백업 폴더를 통째로 비울 수 있었습니다.**
  "손상" 판정에는 *사이드카와 zip 메타를 둘 다 못 읽었다* 가 포함됩니다 — 네트워크 저장소의
  일시적인 I/O 오류이거나, 백업 폴더를 밖으로 동기화하는 중(zip 은 도착했고 사이드카는 아직)
  일 수도 있습니다. 백업본을 외부로 복사하는 것은 이 플러그인이 권하는 운영 방식이라 실제로
  일어납니다. 공간 확보 경로는 이 규칙을 지키고 있었는데 정리 경로에만 빠져 있었고, 하필
  계단식이 기본값이라 **기본 설정에서** 걸렸습니다. 이제 마지막 하나는 남기고 크게 알립니다.
  (읽을 수 있는 백업이 있었다면 `min-backups: 0` 같은 명시적 설정은 그대로 존중합니다)

- **`/wb prune` 이 복원 실패 정지를 무시했습니다.** 복원이 실패하면 플러그인은 자동 백업과
  보관 정리를 멈춥니다 — 반쯤 복원된 월드가 백업되면서 멀쩡한 예전 백업이 정책에 밀려
  사라지는 것을 막기 위해서입니다. 자동 주기·백업 뒤 정리·시작 시 정리·공간 확보는 모두 이
  정지를 지키는데, **정작 그 정책을 직접 부르는 `/wb prune` 만 검사를 빠뜨리고 있었습니다.**
  하필 디스크가 모자란 상황에서 가장 먼저 떠올리는 명령입니다. 이제 거부하고 무엇을 해야
  하는지 알려 줍니다. (`/wb delete [ID]` 는 지울 대상을 직접 지목하므로 그대로 쓸 수 있습니다)
- **`min-free-disk-gb` 에 소수점을 적으면 조용히 0 이 됐습니다.** `0.5` 라고 적어 512MB 를
  남겨 두려던 설정이 "여유 없음" 으로 바뀌어, 디스크 브레이크가 사실상 꺼졌습니다.
  경고도 없어서 백업이 디스크를 끝까지 채운 뒤에야 드러났습니다. 이제 소수점을 받습니다.
- **제외 패턴을 윈도우식(`world\region\**`)으로 적으면 아무것도 걸러 내지 않았습니다.**
  검사할 경로는 `/` 로 맞추면서 패턴은 그대로 뒀기 때문입니다. 조용히 통과해서, 큰 폴더를
  백업에서 뺐다고 믿는 쪽이 실제로는 그대로 담고 있었습니다. 이제 패턴도 같이 정규화합니다.
- **`retention.tiers` 를 항목 형태가 아니게 적으면 경고 없이 옛 정책으로 돌아갔습니다.**
  계단이 도는 줄 알았는데 실제로는 `max-backups`/`max-age-days` 가 돌고 있고, 그 사실은
  정작 되돌려야 하는 날에야 드러납니다. 이제 시작할 때 콘솔에 알립니다.
- **복원 요청과 `/wb confirm` 사이에 자동 백업 주기가 걸리면** 안전 백업이 "이미 백업이
  진행 중" 으로 실패하면서 복원이 중단됐고, 그 메시지만으로는 원인을 알 수 없었습니다.
  이제 확정 단계에서 먼저 알리고 요청을 살려 둡니다 — 백업이 끝나면 `/wb confirm` 만 다시
  입력하면 됩니다.
- 안전 백업이 실패해 복원이 막혔을 때, 빠져나갈 길(`restore.create-safety-backup: false`)을
  함께 알려 줍니다. 되돌릴 방법이 아예 없는 것처럼 보이던 화면이었습니다.

---

## 1.1.0

`1.0.0` 이후의 안정성 강화를 모았습니다. **config.yml 은 그대로 쓰셔도 됩니다** — 플러그인은
설정 파일이 없을 때만 새로 쓰므로, 기존 값과 동작이 유지됩니다.

### 업그레이드하면 바뀌는 것

- **백업에 이 플러그인의 `config.yml` 이 함께 담깁니다.** 담지 않으면 서버를 되돌려도 그 시점의
  보관 정책은 되돌아오지 않아서, 백업이 어떻게 만들어졌는지가 정작 백업 안에 없었습니다.

  > 그래서 **복원하면 `config.yml` 도 그 시점 것으로 바뀝니다.** `backup.directory` 처럼 그 뒤에
  > 고친 값이 예전으로 돌아갈 수 있으니 복원 후 `/wb status` 로 한 번 확인하세요.
  > 원치 않으면 `targets.exclude` 에 `"plugins/WorldBackUp/config.yml"` 을 넣으면 됩니다.
  > 이 버전 이전에 만든 백업으로 복원할 때는 예전처럼 `config.yml` 을 건드리지 않습니다.

- 반대로 플러그인이 **스스로 만들어 쓰는 것**(`backups/`, `replaced/`, 복원 예약 파일, 실패 표식)은
  절대 백업에 담기지 않습니다. 담기면 복원이 그것을 되살리는데, 복원 예약이 되살아나면 다음 부팅이
  **또 복원을 시작하고**, 실패 표식이 되살아나면 자동 백업이 영구히 멈춥니다.
- 기본 보관 정책이 **계단식(`retention.tiers`)** 으로 바뀌었습니다. 새로 설치하는 서버에만 적용되며,
  최근일수록 촘촘히 남겨 "자는 동안 당한 테러를 아침에 발견" 하는 상황을 견딥니다. (기본값 27개 / 약 16일)
- 기본 백업 방식이 **차등(`backup.mode: differential`)** 으로 바뀌었습니다. 새로 설치하는 서버에만
  적용됩니다. `full` 을 쓰면서 깊은 계단을 함께 설정한 서버는 시작할 때 콘솔에 경고가 뜹니다.

### 새로 생긴 것

- `retention.min-backups` — 어떤 정책으로도 이 개수 아래로 줄이지 않는 하한선입니다. 무인 서버가
  오래 놀면 나이 정책 하나로 백업이 전멸할 수 있었습니다. **0 으로 두지 마세요.**
- `retention.max-total-size-gb` — 백업 폴더 총 용량 상한. 컨테이너 호스팅에서는
  `min-free-disk-gb` 가 호스트 디스크를 보므로 정작 걸려 있는 할당량을 못 봅니다.
- `backup.max-skipped-cycles` — 변경이 없어 보여도 이 횟수를 채우면 한 번은 백업합니다.
  "변경 없음" 판단은 블록 설치·파괴·접속만 보므로, 강제 로드된 청크의 농장은 잡히지 않습니다.
- `retention.max-protected` — 수동·복원 직전 백업이 무한히 쌓이지 않게 하는 상한.
- `restore.keep-replaced-max` — `replaced/` 스냅샷 보관 개수. 복원 한 번에 월드 한 벌이 쌓입니다.
- `/wb restore` (인자 없이) — 되돌릴 시점을 클릭으로 고르는 메뉴. 각 줄이 실제로 존재하는 백업을
  가리키므로 눌렀을 때 어디로 돌아가는지가 화면에 그대로 보입니다.
- `/wb restore at [시각]` — `03:00`, `2026-08-16 03:00`, `9h` 로 되돌릴 시점을 찾습니다.
  모두 **"그 시점 이전"** 을 고릅니다. 사고 시각 이후 백업이 잡히면 피해가 담긴 상태로 돌아가기 때문입니다.
- `/wb list days` — 날짜별 보유 현황. 계단식이 실제로 도는지도 이 화면에서 드러납니다.
- `/wb lock` · `/wb unlock` — 자동 삭제로부터 보호. 어떤 정책보다 우선합니다.
- 탭 완성이 종류별로 묶여 나오고, **각 줄에 그것이 실제로 가리키는 백업**이 적힙니다.

### 고친 것

- **인벤토리가 백업에 들어가지 않던 문제.** 26.2 는 `getWorldFolder()` 가 차원 폴더
  (`world/dimensions/minecraft/overworld`)를 돌려주는데, 거기만 담으면 지형은 되돌아오지만
  `playerdata` · `level.dat` · `data` 가 통째로 빠지고 **그런데도 백업은 성공한 것처럼 보였습니다.**
  이제 실제 월드 폴더까지 올라가고, 플레이어 데이터 위치를 전제하지 않고 찾습니다.
  백업에 인벤토리가 들었는지 기록해 `/wb list` · 복원 확인 화면 · `/wb status` 에 표시합니다.
- **서버가 아직 쓰고 있는 region 파일을 압축하던 문제.** 청크 쓰기가 디스크에 끝날 때까지
  기다립니다. 기다리지 않으면 헤더와 데이터가 어긋난 조각이 담겨, 복원 시
  `Corrupt regionfile header` 가 뜨고 그 청크가 재생성됐습니다.
- **백업 중 읽지 못한 파일을 "담았다" 고 기록하던 문제.** 잠긴 파일(`session.lock`, 서버가 쓰는
  중인 region)은 열기는 되고 읽기에서 실패합니다. 이제 담아낸 파일만 목록에 오르므로,
  복원이 멀쩡한 파일을 빈 파일로 덮어쓰지 않고 다음 차등 백업이 그 파일을 빠뜨리지 않습니다.
- **복원이 공간 부족으로 반쯤 진행되던 문제.** 복원은 "비우고 푸는" 순서라 도중에 디스크가 차면
  반쯤 복원된 월드만 남았습니다. 이제 지우기 전에 필요한 양을 정확히 계산해서, 모자라면 월드를
  건드리기 전에 멈춥니다. `/wb restore` 확인 화면에도 필요/남은 공간이 표시됩니다.
- **복원이 실패한 뒤 멀쩡한 예전 백업이 사라지던 문제.** 실패하면 `restore-failed-*.yml` 을 남기고
  그동안 자동 백업과 보관 정리를 멈춥니다. 반쯤 복원된 월드가 계속 백업되면서 되돌릴 수 있는
  백업을 정책으로 밀어내던 연쇄를 끊습니다.
- **총 용량 상한이 최신 백업을 지우던 문제.**
- **차등 백업의 기준이 삭제되어 갓 만든 차등본이 복원 불가가 되던 문제.** 압축 중에는 기준을
  고정하고, 보관 정책·공간 확보·수동 삭제 어느 쪽으로도 지우지 않습니다.
- **쓰다 만 백업 메타데이터를 정상으로 믿던 문제.** 차등 백업이 전체 백업으로 둔갑해, 복원이
  대상 경로를 좁히지 못한 채 바뀐 파일 몇 개를 살아 있는 월드 위에 흩뿌렸습니다.
- **백업이 실패하면서 멀쩡한 백업을 지우던 문제.** 압축이 끝난 뒤 뒷정리에서 난 예외가 방금 만든
  백업을 지우고, 성공한 백업을 실패로 보고했습니다.
- 백업이 어떤 이유로 실패해도(타임아웃 포함) 월드 **자동 저장이 반드시 원복**됩니다.
  꺼진 채 방치되면 서버가 크래시할 때 그 세션 전체가 날아갑니다.
- `worlds` 만 복원할 때 월드를 못 찾으면 전체를 덮어쓰지 않고 거부합니다.
- 백업에 내용이 없는 경로는 비우지 않고 건너뜁니다. (비우기만 하면 그 폴더가 사라집니다)

### 빨라진 것

- 이미 압축된 형식(`.mca`, `.zip`, `.png` 등)은 재압축하지 않습니다. 결과물 크기는 사실상 그대로이고
  백업 시간은 **8배** 짧아졌습니다(31MB 월드: 753ms → 94ms).
- 차등 백업이 대상을 두 번 훑지 않습니다. 공간이 넉넉하다고 단정할 수 있으면 용량 측정 순회를
  건너뜁니다(파일 2만 개 월드에서 114ms → 79ms).
- `/wb list` · `/wb status` · 탭 완성이 매번 백업 폴더를 다시 읽지 않습니다.

---

## 1.0.0

첫 공개. 주기적 백업, 시점 복원, 차등 백업, 보관 정책, `/wb` 명령어.
